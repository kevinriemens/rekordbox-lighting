"""experiments package — disposable, one-off orchestration built on top of
the permanent repo modules (rbxlight.macros.patterns, rbxlight.phrases.repo).
Deliberately isolated so a whole experiment can be deleted in one commit
once it concludes. Never wired into rbxlight.menu.
"""
